%                  IBVS.STY Ver 1.0  LaTeX include file for the
%                    Information Bulletin on Variable Stars,
%                            IAU Comm. 27 and 42
%                 10-07-1992     Katalin Ol\'ah & Andr\'as Holl
%
%                    Konkoly Observatory, Budapest, Hungary

% Use documentstyle "article"
% Include IBVS.STY after the \documentstyle command, but before \begindocument

% IBVS.STY screen message
\typeout{}
\typeout{IBVS.STY <10.07.1992>}
\typeout{Information Bulletin on Variable Stars - Konkoly Observatory}
\typeout{}

\font\ff=cmss12    % omss12 can be used as well
\font\ft=cmbx12    % ombx12 can be used as well

\topmargin=-1truecm
\pagestyle{empty}
\oddsidemargin=0cm
\evensidemargin=0cm
\textheight=24truecm
\textwidth=16truecm

\def\degr{\hbox{$^\circ$}}              % Degree
\def\fh{\hbox{$^{\rm h}$}}              % Fractions of hours
\def\fs{\hbox{$^{\rm s}$}}              % Fractions of seconds
\def\fdg{\hbox{$^\circ$}}               % Fractions of degrees
\def\farcm{\hbox{$.\mkern-4mu^\prime$}}    % Fractions of arcminutes
\def\farcmm{\hbox{$^\prime$}}              % Fractions of arcminutes
\def\farcs{\hbox{$.\!\!^{\prime\prime}$}}  % Fractions of arcseconds
\def\fd{\hbox{$.\!\!^{\rm d}$}}            % Fractions of days
\def\fp{\hbox{$.\!\!^{\rm p}$}}            % Fractions of period
\def\fss{\hbox{$.\!\!^{\rm s}$}}           % Fractions of seconds
\def\fdd{\hbox{$^{\rm d}$}}                % Fractions of days
\def\fm{\hbox{$.\!\!^{\rm m}$}}            % Fractions of magnitudes
\def\fmm{\hbox{$^{\rm m}$}}      % Fractions of magnitude or minutes
\def\ts{\thinspace}                       % Thinspace

% Definition of macro \IBVShead
\def\IBVShead#1#2 {\centerline {\ff COMMISSIONS 27 AND 42 OF THE IAU}
  \centerline {\ff INFORMATION BULLETIN ON VARIABLE STARS}
  \vskip 0.1cm
  \centerline {\rm Number #1}
  \vskip 0.5cm
  \hbox to \textwidth{\hfil
  {\vbox{\hbox{\rm Konkoly Observatory}\hbox{\rm Budapest}
  \hbox{\rm #2}\vskip 0.2cm \hbox{\sl HU ISSN 0324 -- 0676}}}}
  \vskip 0.8cm plus 0cm minus 0.5cm}

% Definition of macro \title
\def\title#1 {\uppercase
  {\centerline {\ft #1} \vskip 1cm plus 0.5cm minus 0.5cm}}

% Definition of macro \begintext
\def\begintext{\large \baselineskip=5mm \noindent}

% Definition of macros \beginauthors - \authorsrightcol - \endauthors
\let\cbb={
\let\cbe=}
\def\beginauthors{\vskip 0cm plus 0cm minus 0cm \hbox to \hsize \cbb
 \hskip 0.5cm \hbox to 6cm \cbb \vbox \cbb \hsize=8cm \obeylines}
\def\authorsrightcol{ \vskip 1mm \cbe \cbe \hskip 2.5cm \hbox to 7cm \cbb \vbox
\cbb
 \hsize=7cm \obeylines}
\def\endauthors{ \cbe \cbe \hfil \cbe}

% Definition of macro \references
\def\references {\vskip 1cm plus 0.5cm minus 0.5cm
\noindent References: \hfil
  \vskip 0.3 cm plus 0.1 cm minus 0.1cm
\parindent=-1cm \leftskip 1cm}
