pdf-extract
================================================================================

Ausgeführte Schritte:
--------------------------------------------------------------------------------
* Checkout: 
	git clone https://github.com/CrossRef/pdfextract
* Installed dependencies: 
	- Installed ruby:	
		sudo apt-get install ruby-dev
	- Installed RVM to be able to downgrade to ruby 2.0.0
		see https://rvm.io/rvm/install
        (Don't forget to run 'source ~/.rvm/scripts/rvm')
	- Installed ruby 2.0.0:
		rvm install 2.0.0
		rvm use 2.0.0
	- Patched pdf-extract.gemspec:
		s.required_ruby_version = "~> 2.0.0"
		s.add_dependency 'pdf-reader', '>= 1.1.1'
	- Nevertheless, pdf-reader version 1.4.0 was installed. 
		gem uninstall pdf-reader
		gem install pdf-reader -v 1.1.1
	- Installed libsqlite:
		sudo apt-get install libsqlite3-dev
		
* Installed pdf-extract:
	gem build pdf-extract.gemspec
        gem install gem install pdf-extract-*.gem

	(OLD (not sure if still valid): sudo gem install pdf-extract)
	
Executable is located in /home/korzen/.rvm/gems/ruby-2.0.0-p648/bin/pdf-extract

Syntax
--------------------------------------------------------------------------------
pdf-extract extract --bodies <PDF-File>

Options:
    -- characters: outputs characters per page.
    -- chunks: outputs chunks (=lines?) per page, but unsorted.
    -- regions: outputs lines per region and page (*)
    -- columns: outputs the coordinates (but no textual content) of columns per page, 
    -- titles: outputs the title
    -- sections: outputs lines per section and page (*)
    -- bodies: outputs the coordinates (but no textual content) of bodies(?), 
