[general]
name = pdftohtml
url = https://poppler.freedesktop.org/
info = Converts PDF files to HTML and XML format retaining formatting.

[run]
cmd = ./bin/pdftohtml -i -xml -zoom 1 -nomerge -stdout -q -enc "UTF-8" $IN > $OUT

[extractor]
name = tool_extractor.PdfToHtmlExtractor
